import { SocketMessageResponseDto } from './socket-message-response-dto';

describe('SocketMessageResponseDto', () => {
  it('should create an instance', () => {
    expect(new SocketMessageResponseDto()).toBeTruthy();
  });
});
