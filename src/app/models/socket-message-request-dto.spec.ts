import { SocketMessageRequestDto } from './socket-message-request-dto';

describe('SocketMessageRequestDto', () => {
  it('should create an instance', () => {
    expect(new SocketMessageRequestDto()).toBeTruthy();
  });
});
