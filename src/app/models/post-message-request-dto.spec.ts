import { PostMessageRequestDto } from './post-message-request-dto';

describe('PostMessageRequestDto', () => {
  it('should create an instance', () => {
    expect(new PostMessageRequestDto()).toBeTruthy();
  });
});
